package com.exco.hostapp.integration.util

object Constants {
    const val CONFIG_BUNDLE_KEY = "configuration"
    const val UI_METHOD = "ui_method"
}

enum class UiMethod {
    ScrollView, RecyclerView, Compose
}
